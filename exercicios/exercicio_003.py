"""

Exercício 3: A Calculadora Formatada
Foco: Entrada (input), Conversão de Tipos, Saída Customizada (sep, end)

O Problema:
Você precisa criar um pequeno script que receba três informações do usuário, faça um cálculo matemático simples e imprima o resultado de uma forma muito específica, sem usar f-strings neste caso, apenas para treinar o domínio do comando print().

Sua missão:

Peça ao usuário (via terminal) para digitar:

Uma palavra qualquer (string).

Um número inteiro (quantidade).

Um número decimal (valor unitário).

Garanta que as variáveis numéricas sejam convertidas para os tipos corretos (int e float).

Calcule o valor total (quantidade * valor unitário) e armazene em uma variável.

O grande desafio: Você deve usar apenas UM único comando print() para exibir a palavra repetida o número de vezes informado (dica: multiplicação de strings), seguida do texto "Total: R$", seguido do valor calculado.

Para separar a palavra repetida, o texto "Total: R$" e o valor, você deve usar o parâmetro sep=" --- ".

Para finalizar a linha, você deve usar o parâmetro end="!!!".

Exemplo de execução:

Palavra digitada: Teste

Quantidade: 3

Valor unitário: 2.5

Resultado esperado: TesteTesteTeste --- Total: R$ --- 7.5!!!

"""

# Solução do exercício
